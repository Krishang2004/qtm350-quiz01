git checkout main && git merge hotflix
git checkout main\n
git merge hotflix
git branch -vv\n
# 1) Make sure you’re on main\ngit checkout main\n\n# 2) Ask Git to resolve the branch name to a commit hash\ngit rev-parse --verify refs/heads/hotfix\n
git checkout main
git rev-parse --verify refs/heads/hotfix
git merge 98381ccc79757f834ae0f280e52973c88048bfa4
git log --oneline
git remote add origin https://github.com/Krishang2004/git-practice.git\ngit branch -M main\ngit push -u origin main
touch commands.txt
git add commands.txt
git commit -m "Add commands.txt" 
git push
cd ..
clear
git clone https://github.com/Krishang2004/qtm350-quiz01.git
cd qtm350-quiz01
touch commands.txt
git checkout -b feature-update
mkdir reports
touch reports/summary.md
echo "This document provides a summary of the project updates." > reports/summary.md
git add reports && git commit -m "Add reports directory"
touch reports/file{1..3}.txt
mv data/raw-data.csv data/input-data.csv
mkdir -p scripts/backup && cp scripts/*.py scripts/backup/
rm docs/documentation.md
git add -A && git commit -m "Update project files"
touch .gitignore
echo "temp/" > .gitignore
echo "dataset0?.csv" >> .gitignore
cat .gitignore
git add .gitignore && git commit -m "Add .gitignore file"\n
git checkout main && git merge refs/heads/feature-update\n
